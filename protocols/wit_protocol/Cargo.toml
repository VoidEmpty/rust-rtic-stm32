[package]
name = "wit_protocol"
version = "0.1.0"
edition = "2021"

[lib]
name = "wit_protocol"
test = false
bench = false

[dependencies]
# parser combinators library
nom = { version = "7.1.3", default-features = false, features = ["alloc"] }
defmt = "0.3.5"
